// Legacy placeholder: this workspace currently runs with Next.js, not Vite.
// Keep this file only to avoid module-resolution errors from old Vite template code.
const config = {};

export default config;
